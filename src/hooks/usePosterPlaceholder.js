import lightPoster from '../../static/placeHolderPosterLight.png';

/**
 *
 * @returns returns a poster placeholder according to the current theme
 */
const usePosterPlaceholder = () => {
  return lightPoster;
};

export default usePosterPlaceholder;
